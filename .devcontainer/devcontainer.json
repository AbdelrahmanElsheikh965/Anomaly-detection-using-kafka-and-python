{
  "name": "Python 3.x with Kafka",
  "postCreateCommand": "pip install -r requirements.txt && sudo apt-get update && sudo apt-get install -y default-jre zookeeperd && curl -s https://downloads.apache.org/kafka/3.0.0/kafka_2.13-3.0.0.tgz | sudo tar -xvz -C /usr/local/",
  "forwardPorts": [9092] 
}
